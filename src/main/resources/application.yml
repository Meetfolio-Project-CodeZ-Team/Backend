spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meetfolio_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kylo
    password: kylo3677
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
  logging.level:
    org.hibernate.SQL: debug